"use client";

// Este archivo no se está usando actualmente. El sidebar se maneja en @/components/ui/sidebar
// Se mantiene una exportación vacía para evitar errores de importación

export function AppSidebar() {
  return null;
}
